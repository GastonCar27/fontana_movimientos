
Si fue en Ubuntu: al servidor le falta el cliente de MySQL (el programa mysqldump no viene instalado por defecto, aparte del servidor de base de datos). Se instala con:

sudo apt update
sudo apt install -y default-mysql-client

No hace falta reiniciar el servicio — probá el backup de nuevo desde la página apenas termine de instala